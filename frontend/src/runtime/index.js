// runtime/index.js — H-Script interpreter bundled for browser
// The interpreter is pure JS — import and use directly in the browser.
// Note: lele (import) is disabled in browser mode.

// TODO: copy/bundle src/lexer.js, src/parser.js, src/interpreter.js here
// and export a single runCode(code) function.

export function runCode(code) {
  // placeholder — will import lexer/parser/interpreter
  return { output: [], errors: [] }
}
